# MShell 智能命令补全系统优化设计

## 概述

优化 MShell 的命令补全系统，使其更加智能和有特色，对标 Fig、Warp、Fish Shell 等业界领先产品。

## 已实现功能

### 1. ✅ 内联预览补全 (Inline Ghost Text)

**效果：**
```
用户输入: cd /va
终端显示: cd /va|r/log  ← 灰色预览文本
按 Tab/→: cd /var/log|  ← 接受补全
```

**实现文件：**
- `src/components/Terminal/GhostText.vue` - 内联预览组件
- `src/utils/autocomplete/inline-suggest.ts` - 内联建议逻辑

**特点：**
- 基于命令历史的智能预测
- 基于 Registry 的命令补全
- Tab 或 → 键接受补全
- 与弹窗补全共存

### 2. ✅ AI 自然语言转命令 (# 触发)

**效果：**
```
用户输入: # 查找大于100M的文件
AI 建议: find . -type f -size +100M
         [Enter 执行] [Tab 编辑] [Esc 取消]
```

**实现文件：**
- `src/components/Terminal/AICommandSuggest.vue` - AI 命令建议组件

**特点：**
- 自然语言描述转 shell 命令
- 危险命令风险提示
- 命令解释说明
- 支持直接执行或编辑

### 3. ✅ 智能上下文感知

**效果：**
```
检测到 Git 仓库:
  🚀 git add . && git commit -m "" && git push
  🔄 git pull --rebase origin main
```

**实现文件：**
- `src/utils/autocomplete/context-detector.ts` - 上下文检测器
- `src/components/Terminal/ContextHints.vue` - 上下文提示组件

**支持的项目类型：**
- Git 仓库
- Node.js 项目 (package.json)
- Python 项目 (requirements.txt, pyproject.toml)
- Docker 项目 (Dockerfile, docker-compose.yml)
- Rust 项目 (Cargo.toml)
- Go 项目 (go.mod)
- Java/Maven 项目 (pom.xml, build.gradle)

**特点：**
- 连接成功后自动检测目录上下文
- 在终端底部显示相关快捷命令
- 点击即可填充命令到终端
- 8秒后自动隐藏，可手动关闭

## 使用方式

| 功能 | 触发方式 | 操作 |
|------|----------|------|
| 内联预览 | 自动（输入时） | Tab/→ 接受 |
| AI 命令 | `#` + 描述 | Enter 执行, Tab 编辑, Esc 取消 |
| 弹窗补全 | 自动（输入 >= 2 字符） | ↑↓ 选择, Tab 确认 |
| 快捷命令 | `/` + 名称 | Tab 确认 |
| 上下文提示 | 自动（连接后检测） | 点击执行 |

## 文件结构

```
src/
├── components/Terminal/
│   ├── TerminalTab.vue           # 集成所有补全功能
│   ├── CommandAutocomplete.vue   # 弹窗补全
│   ├── GhostText.vue             # 内联预览组件
│   ├── AICommandSuggest.vue      # AI 命令建议组件
│   └── ContextHints.vue          # 上下文提示组件
└── utils/autocomplete/
    ├── registry.ts               # 命令注册表
    ├── inline-suggest.ts         # 内联建议逻辑
    ├── context-detector.ts       # 上下文检测
    ├── types.ts                  # 类型定义
    ├── definitions/              # 命令定义 (65+)
    └── providers/                # 数据提供者
```
